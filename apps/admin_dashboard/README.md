# Admin Dashboard

Admin dashboard for BUS TRACKING SYSTEM.

## Stack
- HTML/CSS/JS
- Vite dev server

## Run

```bash
npm install
npm run dev
```

Default local URL:
- `http://localhost:5173`

The page expects the Worker API base URL, defaulting to:
- `http://127.0.0.1:8787`
