# Mobile App

Flutter app for Passenger and Driver.

## Planned Structure

- `lib/main.dart`
- `lib/screens/` — UI screens
- `lib/widgets/` — reusable widgets
- `lib/models/` — data models
- `lib/services/` — API and realtime services
- `lib/config/` — environment and app config

## Suggested Packages

- `http`
- `google_maps_flutter`
- `supabase_flutter`
- `geolocator`
- `flutter_riverpod` or `provider`

Flutter SDK is not installed on this machine yet, so dependencies have not been fetched automatically.
